## smartling-cli glossaries create

Glossary create

### Synopsis

Create a new glossary in the current account.

The glossary name is taken from the positional argument and maps to the API
request's "glossaryName" field. All other API fields are exposed as flags.

Fallback locales are repeatable and use the format
  --fallback-locale <fallbackLocaleId>:<localeId>[,<localeId>...]
e.g. --fallback-locale es:es-MX,es-AR.

```
smartling-cli glossaries create <glossaryName> [flags]
```

### Examples

```

# Create a glossary named "CLI glossary" with two locales

  smartling-cli glossaries create "CLI glossary" --locale es-ES --locale fr-FR

```

### Options

```
      --description string            Glossary description.
      --fallback-locale stringArray   Fallback locale mapping (repeatable). Format: '<fallbackLocaleId>:<localeId>[,<localeId>...]'.
  -h, --help                          help for create
      --locale stringArray            Target locale ID (repeatable → localeIds).
      --verification-mode             Enable verification mode for the glossary.
```

### Options inherited from parent commands

```
  -a, --account string               Account ID to operate on.
                                     This option overrides config value "account_id".
  -c, --config string                Config file in YAML format.
                                     By default CLI will look for file named
                                     "smartling.yml" in current directory and in all
                                     intermediate parents, emulating git behavior.
  -k, --insecure                     Skip HTTPS certificate validation.
      --operation-directory string   Sets directory to operate on, usually, to store or to
                                     read files.  Depends on command. (default ".")
      --output string                Output format: table, json, simple (default "simple")
  -p, --project string               Project ID to operate on.
                                     This option overrides config value "project_id".
      --proxy string                 Use specified URL as proxy server.
      --secret string                Token Secret which will be used for authentication.
                                     This option overrides config value "secret".
      --show-config                  Print the resolved account, project, user, and config file path
                                     to stderr before the command runs.
      --smartling-url string         Specify base Smartling URL, merely for testing
                                     purposes.
      --user string                  User ID which will be used for authentication.
                                     This option overrides config value "user_id".
  -v, --verbose count                Verbose logging
```

### SEE ALSO

* [smartling-cli glossaries](smartling-cli_glossaries.md)	 - Manage Smartling glossaries

###### Auto generated by spf13/cobra on 4-Jun-2026
